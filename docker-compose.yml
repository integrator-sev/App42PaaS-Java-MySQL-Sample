---
version: "3.5"
services:
  mysql:
    image: mysql:5.7.5
#    name: mysql5
    ports:
      - "3306:3306"
    volumes:
      - ./sql:/var/lib/mysql
    environment:
          MYSQL_ROOT_PASSWORD: secret
          MYSQL_DATABASE: dd
          MYSQL_USER: root
          MYSQL_PASSWORD: secret

  tomcat:
    image: tomcat:8.5.59-jdk11
#    name: tom
    ports:
      - "9999:8080"
    volumes:
      - apps:/usr/local/tomcat/webapps
    depends_on:
      - mysql

  maven:
    build: mvn
    volumes:
      - apps:/webapps
    depends_on:
      - tomcat

volumes:
  apps:
